/**
 * `LoadingDetails` is a functional React component that displays a simple loading message.
 *
 * @component
 * @returns {JSX.Element} A `div` element containing the text "Loading".
 *
 */
function LoadingDetails() {
    return (
        <>
            <div> Loading </div>
        </>
    )
}

export default LoadingDetails;
